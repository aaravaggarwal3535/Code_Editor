{
  "name": "code-editor-backend",
  "version": "1.0.0",
  "description": "Backend service for Code Editor with support for multiple languages",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "groq-sdk": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
